{
    "framework": "nextjs",
    "buildCommand": "next build --webpack",
    "outputDirectory": ".next",
    "installCommand": "npm install",
    "functions": {
        "src/app/api/**/*.ts": {
            "maxDuration": 30
        }
    }
}