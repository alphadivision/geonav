import NavigationMapClient from './components/NavigationMapClient';

// Server component — composes the navigation map screen
// No AppLayout needed — this is a full-screen map application

export default function NavigationMapPage() {
  return <NavigationMapClient />;
}